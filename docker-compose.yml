services:
  mongo:
    image: mongo:7
    container_name: itsm-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - itsm-mongo-data:/data/db

volumes:
  itsm-mongo-data:
